# Product Store SPA

Тестовое задание: Single Page Application для управления продуктами.

## Стек технологий

- **TypeScript** 5.9.3
- **React** 19.1.1
- **Vite** 7.1.12
- **Zustand** - state management с персистентностью
- **React Router** v7 - роутинг
- **shadcn/ui + Tailwind CSS** - UI компоненты
- **React Hook Form + Zod** - формы с валидацией
- **DummyJSON API** - источник данных

## Функционал

### Обязательные задачи ✅

1. **Страница /products**
   - Список продуктов с DummyJSON API
   - Лайки (иконка сердечка с подсветкой)
   - Удаление карточек
   - Фильтр "Все продукты" / "Избранное"
   - Одинаковая высота карточек (обрезка текста)
   - Переход на детальную страницу по клику

2. **Страница /products/:id**
   - Детальная информация о продукте
   - Галерея изображений
   - Кнопка возврата на главную страницу

3. **Страница /create-product**
   - Форма создания продукта
   - Обязательные поля с валидацией
   - Сохранение в Zustand store

### Бонусные задачи ✅

- ✅ **Пагинация** - постраничная навигация списка
- ✅ **Редактирование** - можно редактировать созданные продукты
- ✅ **Фильтрация по категориям** - 8 категорий из API
- ✅ **Поиск без кнопки** - с debounce 500ms

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Preview production build
npm run preview
```

После запуска приложение доступно по адресу: http://localhost:5173

## Структура проекта

```
src/
├── components/
│   ├── ui/              # shadcn UI компоненты
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   ├── label.tsx
│   │   └── badge.tsx
│   ├── Layout.tsx       # Общий layout с навигацией
│   ├── ProductCard.tsx  # Карточка продукта
│   ├── ProductFilter.tsx # Фильтры
│   └── SearchBar.tsx    # Поиск с debounce
├── pages/
│   ├── ProductsPage.tsx      # /products - список
│   ├── ProductDetailPage.tsx # /products/:id - детали
│   └── CreateProductPage.tsx # /create-product - форма
├── store/
│   └── productStore.ts  # Zustand store с persist
├── api/
│   └── products.ts      # API клиент для DummyJSON
├── types/
│   └── product.ts       # TypeScript типы
├── hooks/
│   └── useDebounce.ts   # Custom hook для debounce
└── lib/
    └── utils.ts         # Утилиты (cn function)
```

## Особенности реализации

### 🏗️ Архитектура (Senior Level)

**Переиспользуемые хуки:**
- `useProducts` - доступ к продуктам и состоянию загрузки
- `useProductActions` - все действия с продуктами (like, delete, create)
- `useProductFilters` - состояние фильтров
- `useDebounce` - debounce для поиска

**Оптимизация производительности:**
- ✨ **Lazy Loading** - страницы загружаются по требованию (code splitting)
- ✨ **React.memo** - ProductCard мемоизирован для предотвращения лишних ре-рендеров
- ✨ **Suspense** - плавная загрузка с красивыми лоадерами
- ✨ **ErrorBoundary** - перехват ошибок без краха приложения
- ✨ **Skeleton Loaders** - красивые плейсхолдеры во время загрузки



### Валидация форм

- React Hook Form для управления формой
- Zod для схем валидации
- Все поля обязательные с минимальной валидацией:
  - Title: минимум 3 символа
  - Description: минимум 10 символов
  - Price: больше 0
  - Category, Brand: минимум 2 символа
  - Thumbnail: валидный URL

### UI/UX

**Анимации и микроинтеракции:**
- 🎨 Плавные переходы на карточках (hover: scale, translate, shadow)
- 🎨 Fade-in анимации для диалогов
- 🎨 Skeleton loaders с pulse эффектом
- 🎨 Спиннер с ping эффектом

**Удобство использования:**
- Адаптивный дизайн (grid с responsive columns)
- Confirm dialog перед удалением
- Loading состояния с красивыми анимациями
- Error handling с понятными сообщениями
- Одинаковая высота карточек через CSS line-clamp
- Галерея изображений на детальной странице
- Lazy loading изображений
- Accessibility (aria-labels, keyboard navigation)
